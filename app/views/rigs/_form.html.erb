<%= form_with(model: rig) do |form| %>
  <% if rig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rig.errors.count, "error") %> prohibited this rig from being saved:</h2>

      <ul>
        <% rig.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="form-group">
    <%= form.label :image %>    <%= form.file_field :image, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
